#include <stdio.h>
#include <time.h>
#include <immintrin.h>

int main(void)
{
	/* TODO: do some time measurements: read one int from /dev/(u)random,
	 * RDRAND, RDSEED, then more ints. Observe the behaviour.
	 * Maybe do some graphs, too.
	 */

	return 0;
}
